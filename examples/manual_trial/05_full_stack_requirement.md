# TestConductor 本地全渠道回归

在 local 环境完成一次复合测试，并为每个渠道保留独立结果和证据。

## UI 页面测试

使用当前所选 Procedure 资产库中已经发布的 Workflow Lab 登录模块完成登录，登录后页面应显示已登录用户和角色。

## 接口测试

调用 TestConductor 健康检查接口，HTTP 状态码必须为 200。

## 数据库测试

只读检查 TestConductor 数据库中的 Django 迁移记录，迁移记录数量必须大于 0。

## 性能测试

对已登记的健康检查目标执行轻量负载，HTTP 错误率必须为 0，响应延迟 p95 不得超过 500 ms。

## TCP 端口测试

TestConductor 本地服务的 TCP 8007 端口必须可以连接，连接耗时应能被记录。

测试不得修改数据库，不创建业务数据，不需要清理资源。
